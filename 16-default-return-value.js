function getLandmark( city ) {
    if( city === 'Mumbai' ) {
        return 'CST'
    }

    // return undefined;
}

console.log( getLandmark( 'Mumbai' ) );
console.log( getLandmark( 'Delhi' ) ); // undefined